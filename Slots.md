# 🎰 Slots

```js
const { Slots } = require('grootie.minigames')

new Slots({
	message: message,
	slash_command: false,
	embed: {
		title: '🎰 Slot Machine',
		color: '#5865F2'
	}
}).startGame();
```